<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ShiftWiz</title>
  <link rel="stylesheet" type="text/css" href="/css/jass.css">
  <link rel="stylesheet" type="text/css" href="/css/style.css">
</head>

<body>
  <header>
    <nav class="menu">
      <img src="/images/ShiftWizWIP.png" alt="Logo" class="logo">
      <ul>
        {{#if loggedIn}}
        <li><button id="schedule" class="btn-no-style">Schedule</button></li>
        <li><button id="Profile" class="btn-no-style">Profile</button></li>
        <li><button id="logout" class="btn-no-style">Logout</button></li>
        {{else}}
        <li><a href="/login">Login</a></li>
        {{/if}}
      </ul>
    </nav>
  </header>
  <main>
    <div class="gallery">
      {{{body}}}
    </div>
  </main>
  {{#if loggedIn}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>
